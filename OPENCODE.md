# OPENCODE.md

opencode entrypoint for this repository.

Use [AGENTS.md](AGENTS.md) as the canonical instruction file. It contains the project source of
truth, required checks, live-runtime guard, security rules, and operating commands.

Do not duplicate rules here. If opencode-specific behavior is needed later, add only the minimal
delta below and keep it consistent with `AGENTS.md`.
